# 业务规则（冻结语料，不要改）

这份文件是 `idNamespaces` 里 `BR` 前缀的 `definedIn` 目标。字典里引用的每个
BR 号都必须在这里有定义，否则 checker 报悬空引用。

- **BR-REFUND-001** 退款必须先校验原支付流水存在。
- **BR-REFUND-002** 超过截止时间的退款不自动放行。截止时间本身未定，见 G-01。
- **BR-RATE-001** 多币种金额一律以最小货币单位存储。
- **BR-RATE-002** 汇率换算的取整方向由本文件规定，见 G-02。
