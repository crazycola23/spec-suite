<!-- 模板：升级到 L2（有前端且要前后端并行）时拷入，放页面规格目录，命名门风如 40-screens/10-总览.md。
     本文件定义「每屏规格长什么样」和「所有屏共享的全局规则」—— 各屏不重复写这些，写了就是复制漂移。
     本层新增两个 ID 命名空间：S-xx（屏）、M-xx（弹窗），必须同时加进 spec-suite.config.json 的 idNamespaces，
     definedIn 分别指向本文件/弹窗规格与各 S<nn> 文件。
     追溯矩阵要求每条 BR/AC 至少落到一个 S 或 M —— 用 coverageRequirements 把它变成检查 7 的断言。 -->

# <项目代号> · 页面规格总览

## 1. 屏清单

| 屏 | 名称 | 菜单码 | 一句话 |
|---|---|---|---|
| S01 | <登录页> | <无菜单码> | <所有人第一个看到的页面> |
| S02 | <工作台> | <menu:code> | <聚合待办与指标> |
| S03 | <项目列表> | <menu:code> | <一句话> |

（行数由本表自己派生。别在别处复述"共 N 屏" —— 铁律 2。）

## 2. 每屏规格的固定骨架

所有 `S<nn>-*.md` 都用这个结构，字段含义统一：

```
路由 / 菜单码 / 视图权限      ← 顶部代码块，含本屏用到的接口与 Tab 清单
—— 核心断言 ——               ← 1~3 句只属于本屏的规则（引用 BR/AC ID）
—— 区块 ——                   A/B/C… 从上到下
—— 表格列 ——                 列名 ← 数据来源（表.字段），标明快照 or 关联
—— 动作矩阵 ——               状态 → 允许的按钮 → 权限码 → 打开的弹窗
—— 五态 ——                   加载 / 空 / 无权限 / 错误 / 部分失败
—— 关联 AC ——                该屏必须满足的 AC / NFR
—— 缺口 ——                   本屏受哪些 D-* / G-* 阻塞
```

## 3. 全局规则（每屏都适用，各屏不重复写）

### 3.1 五态定义

| 态 | 要求 | 依据 |
|---|---|---|
| **加载** | 骨架屏；长任务显示排队/处理中/进度 | <依据 ID> |
| **空** | **必须给下一步引导**，禁止只写"暂无数据" | <依据 ID> |
| **无权限** | 动作列**整列隐藏**；不渲染禁用按钮、不弹错误 | <依据 ID> |
| **错误** | 按 `bizCode` 分支渲染；展示 `requestId` 可复制 | <conventions §N> |
| **部分失败** | 批量操作**逐条标原因**，成功项可继续使用 | <依据 ID> |

**五态是 DoD**：缺任一态，对应任务切片不算完成。

### 3.2 高风险动作统一交互

任何标 ⚠ 的高风险按钮点击后必须先开确认弹窗，弹窗内**必须**同时展示：

1. 影响范围（对象名 + 数量）
2. <涉及费用的项目保留：预计费用 + 命中的预算桶 + 桶当前可用>
3. 不可撤回项原文
4. 缺失的前置条件（逐条，不合并成"条件不满足"）

### 3.3 渲染红线

| # | 红线 | 依据 |
|---|---|---|
| R-1 | <未知态> 的行**不允许出现任何重试入口**，只出查单/查回执 | <依据 ID> |
| R-2 | `not_measured` 不得渲染为 `是` / `否` / `0` / `—`，必须是可视觉区分的第三态 | <依据 ID> |
| R-3 | N/A 指标不得显示 0，必须显示 `N/A` + 原因码 | <依据 ID> |

（红线条目按项目增删。判据同 N-05~N-07：指不出一条真实出事路径的红线，删 —— 一条指不出风险的禁令只会把注意力引到不存在的地方。）

### 3.4 按钮可用性来自服务端，不来自前端推断

列表接口返回 `allowedActions[]`，前端**按它渲染**。
禁止前端写 `if (status.includes('失败')) 显示重试` —— 这类推断是 R-1 类缺陷的主要来源。
权威动作矩阵在 `contracts/dictionary.yaml` <的 retryMatrix，若声明>。

### 3.5 <费用字段的可见性>

<不涉及金额的项目整节删除。>所有金额列 / 卡片受 <perm:code> 控制。无该权限时**整列隐藏**，不显示 `***`。

## 4. <侧边栏 / 导航>

<本项目菜单结构的一句话描述；不需要就删本节。>

## 5. 本层的已知缺口

<本层被哪些 G-* 卡住。没有就写"无"，不要预填。>
