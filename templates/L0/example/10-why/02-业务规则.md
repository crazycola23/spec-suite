# 业务规则（示例真相源）

本文件是这个最小示例的唯一业务真相源。契约层只引用这里的 `BR-*` ID，不复制下面的中文描述。

### BR-MEMBER-001 成员状态取值

成员有「生效」「已移除」两种状态；新加入的成员默认生效。

### BR-MEMBER-002 移除后进入终态

成员被移除后不可再参与项目，状态不可逆。

### BR-MEMBER-003 邀请状态机

邀请从「待接受」开始。

### BR-MEMBER-004 接受邀请

待接受的邀请被接受后进入「已接受」终态。
