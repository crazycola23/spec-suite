# 模板：拷到规格库根目录。

node_modules/

# ---------------------------------------------------------------------------
# 下面这些**不要**加进来，加了会拆掉机制本身：
#
#   generated/        必须签入。消费仓库的 verify:contracts 就是拿签入副本和它比对；
#                     不签入 = 比对对象不存在 = 检查 4 与整条闸门变成空跑。
#   report.json       审计报告是一次性产物，本来就不该在规格库里；别用 ignore 掩盖
#   report.md         "把报告写进了活库"这件事 —— 改成 --report 指向库外的目录。
# ---------------------------------------------------------------------------
