<!-- 模板：每块屏一份，命名 S<nn>-<名称>.md。骨架必须与总览 §2 一致 —— 读者靠固定结构找东西。
     所有业务事实引用 BR / AC / 错误码 / 权限码 ID，不复制中文描述（N-03）。
     "数据来源"写 表.字段；派生列写派生规则引用的 BR ID，不在本文件重述公式。
     拷入后 checker 会把残留 <...> 占位符报为缺陷 —— 半填的规格比没有规格更危险。
     本文件是 S-xx 的定义处之一（config 里 definedIn 要含 S<nn>-*.md 的 glob）。 -->

# S<nn> · <屏名>

```
路由 <path>（Tab1 支持 `?<query>=<a>|<b>`）   菜单码 <menu:code>   视图权限 <perm:code>
接口 <METHOD /path> · <METHOD /path> · <METHOD /path>
Tab  <tab1> | <tab2> | <tab3>
```

> 核心断言：**<只属于本屏的一句话规则>**（`<BR-XXX-001>`）。
> <第二条断言，可省略。>

## 区块

| 区块 | 内容 |
|---|---|
| A | <头部摘要 / KPI> |
| B | <主体区> |
| C | <…> |

## Tab 1 · <名称>（<主对象>）

| 列 | ← 数据来源 |
|---|---|
| <列名> | `<table>.<field>` + `{id}` |
| <筛选列> | `<query 参数>`（<枚举名>，默认展示全部） |
| <派生列> | 由 <主对象> 派生（`<BR-XXX-00n>`）；不渲染独立状态 |
| <费用列> | `<table>.<field>` —— 受 <perm:code> 控制（总览 §3.5） |

**动作**：<主行动> → **M<nn>**（需 `<perm:code>`）；按钮由服务端 `allowedActions[]` 驱动，不按状态推断（总览 §3.4）。

### <写入边界 / 本 Tab 特有规则>

1. <创建只提交什么；服务端派生什么、不做什么>
2. <预览 → 确认的两段式；确认只提交 version + 预览 ID>
3. <写动作的幂等要求>

## Tab 2 · <名称>（<主对象>）

<同构。没有多 Tab 的屏删掉 Tab 结构，区块直接用 ##。>

### 动作矩阵 —— 唯一权威来源是 <allowedActions[] / 字典 retryMatrix>

| 状态 | 允许的按钮 | 禁止出现 |
|---|---|---|
| <failed 可重试> | 「<重试>」 | — |
| <failed 不可重试> | 「<转人工>」 | **任何重试入口**（`<ERR-XXXXX>`） |
| <未知态> | 「<先查单>」 | **任何重试入口**（`<ERR-XXXXX>`） |
| <进行中> | 只读 + 进度 | 重试 |

**这张表就是 `<AC-XXX-00n>` 的验收脚本。**

## <渲染红线（本屏特有）>

| # | 规则 | 依据 |
|---|---|---|
| 1 | <本屏特有的禁渲染规则> | <BR-XXX-00n>、<AC-XXX-00n> |

<通用红线在总览 §3.3，此处只写本屏特有的。没有就删本节。>

## 五态

| 态 | 行为 |
|---|---|
| 加载 | <骨架屏；进行中行显示进度> |
| 空 | <引导创建 / 引导先做什么> |
| 无权限 | 无 <perm:code> → <哪些列/按钮整列隐藏> |
| 错误 | `<ERR-XXXXX>` / `<ERR-XXXXX>` 分别渲染 |
| 部分失败 | <逐条标原因的场景> |

## 关联 AC

`<AC-XXX-001>`～`<AC-XXX-00n>`、<跨屏 AC / NFR>。

## 缺口

| ID | 影响本屏 | 当前保护性行为 |
|---|---|---|
| `<D-01>` <✅/open> | <影响> | <保护性行为> |
| `<G-01>` | <影响> | <保护性行为> |

（没有阻塞就写"无"。）
