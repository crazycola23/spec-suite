<!-- 模板：所有弹窗一份，命名门风如 40-screens/20-弹窗规格.md。弹窗从属于屏，不独立成屏。
     索引表是 M-xx 唯一的定义处 —— config 里 M 命名空间的 definedIn 指向本文件。
     只读弹窗必须有「⛔ 本弹窗无任何写操作」小节：这是给实现者的硬边界，不是备注。
     高风险弹窗标 ⚠，交互按总览 §3.2 的四项统一规则，各弹窗不重复抄那四项。 -->

# <项目代号> · 弹窗规格 M01–M<nn>

## 索引

| ID | 名称 | 所属屏 | 主对象 | 权限码 |
|---|---|---|---|---|
| M01 | <新建 X> | S03 | <X> | `<perm:code>` |
| M02 | <确认 X> ⚠ | S03 / S04 | <X> | `<perm:code>` ⚠ |
| M03 | <快照（只读）> | S07 | <X> | `<perm:code>` |

⚠ = 高风险动作（统一交互见总览 §3.2）。索引行数派生，别处不复述总数（铁律 2）。

## 所有确认类弹窗的共同规则

| 情形 | 行为 |
|---|---|
| 任一前置项 ❌ | 按钮禁用；提交则 `<ERR-XXXXX>` + `details.<field>` 定位字段 |
| 重复提交 | `<ERR-XXXXX>` → <跳转既有对象，不再次创建>（`<AC-XXX-00n>`） |
| 版本冲突 | `<ERR-XXXXX>` + `changedFields` |
| 成功 | <状态迁移 / 建立的待办> |

## M01 · <新建 X>

<表单字段与约束。字段名用 OpenAPI schema 名，不另造中文字段名。>
<服务端派生的字段在此列明"客户端不提交"：status、version、时间戳、编号等。>

## M02 · <确认 X> ⚠

<确认弹窗展示总览 §3.2 的四项；此处只写本弹窗特有的内容：确认后发生什么、是否可撤回。>

## M03 · <快照（只读）>

### ⛔ 本弹窗无任何写操作

<为什么只读：一句话 + 依据 ID。API 无对应 POST/PUT/DELETE，前端无独立路由。>
