<!-- 模板：有历史代码要停用（L3）时拷入，命名门风如 50-delivery/16-旧版停用清单.md。
     旧产物零裁决权（README 裁决表最末级）。本文件只记录代码边界与停用方式：
     不新增业务规则，也不删除历史数据 —— 删数据 / 删表需要单独的迁移决策，不是"停用"的副产品。 -->

# <项目代号> · 旧版 <X> 停用清单

> 本文件只记录代码边界与运行时停用方式，不新增业务规则、状态、权限、阈值、价格或第三方行为。
> 旧版不再作为规格、接口、页面或实现依据；引用旧版任何数字 / 文案 / 页面作为实现依据即缺陷。

## 1. 停用原则

- 当前实现只允许使用 <允许的服务端包 / 前端目录清单>。
- 除本清单明确标记为"已清理"的入口外，其余旧版源码统一标记为"已停止使用"；默认运行时不加载旧 <Bean / 路由>，前端动态菜单不生成旧页面路由。
- 不删除历史数据库表或已有菜单数据；数据库清理需要单独的迁移需求和确认。
- <共享前缀（如 /api/**）不能按前缀整体停用；必须按旧版包、组件和具体路径隔离。>

## 2. 服务端旧版边界

以下路径属于旧版，不得新增调用或继续扩展。<已清理的入口注明"已清理"，保留在清单里用于历史追溯，不代表源码仍存在：>

```text
<com.x.y.controller.old-module>
<com.x.y.service>
```

旧版接口示例：

```text
</old/path/a>
</old/path/b>
```

仍保留的旧版 <Bean> 使用 `<LegacyDisabled 标记>`，仅显式启用 <legacy profile> 时才加载；默认环境不得启用该 profile。

## 3. 前端旧版边界

```text
<views/old-module>
<api/old-module>
```

<已清理的目录注明"已清理"。前端动态菜单仍过滤 <旧路径>，防止数据库里的旧菜单行指向不存在的组件 —— 这是护栏，不是替代实现。>

## 4. 菜单与数据库边界

- <旧菜单种子脚本> 已整体注释并标记为不可执行。
- 已存在数据库中的历史菜单行和旧表不在停用范围内删除；当前前端通过组件过滤避免旧菜单进入运行时路由。
- <基线 DDL 同时包含历史表和现行表时，不得因本清单直接删除或重建 —— 改 DDL 走迁移决策。>

## 5. 验证要求

- 服务端检查仍保留的旧控制器 / 服务均带 <LegacyDisabled 标记>，并确认已清理的 <包 / 类> 不存在。
- 前端测试验证旧版路由被过滤、<现行路由> 保留，并确认已清理目录不存在。
- 规格检查、生成物漂移校验、fixtures 测试继续通过。

## 6. 本轮清理边界（<YYYY-MM-DD>）

- 清理：<本轮删除了哪些旧入口（控制器 / 服务 / 页面 / API 目录）>
- 保留：<哪些现行契约 / 表 / 占位码不动，为什么>
- 菜单与历史数据：<维持什么现状>
