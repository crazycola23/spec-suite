<!-- 模板：有第三方非确定性接口（外部平台 / Provider / 付费 API）时拷入，命名门风如 50-delivery/13-外部依赖基线.md。
     本文件的核心价值恰恰是"待补"：外部合同事实未冻结时，用占位编号 + 待补登记，
     而不是抄原型 / 宣传页 / 文档里的示意名 —— 那是 N-01 最高危的形态（编造对外事实，且不可撤销）。
     填表的唯一合法依据：商务合同或沙箱联调证据。
     占位编号（如 X-P01..P08）要加进 config 的 idNamespaces，标 mayLackDefinition —— 它们被引用但"定义"就是本表的待补行。 -->

# <项目代号> · <Provider / 外部依赖> 合同基线

> 抽自 <真相源 / 技术设计 §N>。
> **<平台 / 供应商> 名称不得由测试或代码临时决定**（`N-05` 类禁令）。

## 0. 当前状态：<未冻结 🔴 / 部分冻结 🟡 / 已冻结 🟢>

**当前没有足够事实确定 <正式名称 / 价格 / 退款语义>**，以 `<前缀>-P01`～`P<nn>` 占位。
签约 / 联调后才替换为正式名称并回填 <表>；不得把原型、文档示意名或模型记忆里的名字当作生产合同。

> ⚠️ **缺任一关键项的 <平台 / 供应商> 不得计入"已支持"数量**（<NFR / AC ID>）。
> 代码里**不得**出现任何硬编码的 <平台名 / 单价>（`N-05`）。

## 1. 能力矩阵（进入全链路验收前必须全部冻结）

| 编号 | 正式名称 | Provider | Contract Version | 状态语义已确认 | 单价 / 退款 | 外部标识 / 订单号 | 限流 / 超时 | 证据能力 | 沙箱 / 低成本环境 | Ready |
|---|---|---|---|:--:|---|---|---|---|---|:--:|
| <前缀>-P01 | 待补 | 待补 | 待补 | ❌ | 待补 | 待补 | 待补 | 待补 | 待补 | ❌ |
| <前缀>-P02 | 待补 | 待补 | 待补 | ❌ | 待补 | 待补 | 待补 | 待补 | 待补 | ❌ |

<行数 = 声明要覆盖的外部数量（该数量本身要有出处），派生，不在别处复述。>

## 2. 准入 <N> 项（<真相源 §N>）

| # | 要求 | 关联 |
|---|---|---|
| 1 | API Contract Version 已冻结，<状态码映射可验证> | <ID> |
| 2 | 能以稳定外部标识**查单 / 恢复**；**网络未知不得走通用重试** | <ID> |
| 3 | 单价、计价单位、退款 / 冲正和对账凭据已确认 | <ID> |
| 4 | 限流、轮询间隔、最大任务时长和沙箱方案已验证 | <ID> |
| 5 | <证据能力明确；无法取得时必须可标记为未测量 / 未知，不得伪装完整> | <ID> |

## 3. 已知的接口语义（未经沙箱验证）

| 外部状态码 | 文档中的语义 | 映射到 |
|:--:|---|---|
| <0> | <查询中> | <内部状态> |
| <1> | <成功> | <内部状态 / 字段> |
| <3> | <失败且退款> | <内部状态> → <可重试 N 次> |

### 未映射情形的保护性处理

| 情形 | 处理 |
|---|---|
| 超时 / 断网 / HTTP 5xx | <进入未知态，只能查单> |
| 文档未列出的状态码 | 同上（保护性），**不得猜测语义** |
| <可推断扩展信号的返回字段> | **不得**映射为正式 true/false，统一 <未测量态> |

## 4. 适配器端口（业务模块只依赖这些，不依赖任一家字段）

```
<ProviderRole>  submit(request) → ExternalRequestRef
                query(ref)      → <Result>
                <cancel(ref)    → CancelResult>
```
