# 填 CLAUDE.md 的判断题

`templates/L0/CLAUDE.md` 是要交付的产物，里面的通用部分照抄即可。**这份文件只讲那个模板里需要判断的槽位怎么填** —— 哪些禁令必须重写、保护性默认怎么选、断言表怎么如实填。

（模板里的固定文本不在本文重复。要看某条禁令的正文，去模板。）

---

## 1. 长度是硬约束

目标 150~200 行。`CLAUDE.md` 是唯一必然进入 coding agent 上下文的文件 —— **它的长度上限就是它的有效性上限**。超了就把内容下沉到 `contracts/conventions.md`，那份是查阅型文档，可以长。

判断标准：一条内容如果不需要 agent 每次都记住，就不该在 `CLAUDE.md` 里。

---

## 2. 通用禁令 vs 领域禁令

模板里 `N-01`~`N-04`、`N-11`、`N-12` **逐字通用**，任何项目照抄，不要改措辞（改了就得重新想断言）。

`N-05`~`N-07` 是**领域槽位**，必须按项目重写。抄错的领域禁令比没有禁令更糟 —— 它把 agent 的注意力引到不存在的风险上，同时让人以为风险已被覆盖。

四个问法，每个的回答对应一条禁令：

1. 哪些值一旦编造会造成**不可撤销的对外后果**？（付款、发布到外部平台、对客户发信、下单、扣费）
2. 哪些对象是审计或台账，只许追加？—— 要列出**具体清单**，不写"审计类表"
3. 哪些动作绝不允许由模型输出直接触发？—— 同样要列清单
4. 有没有本项目特有的不可信输入通道？（模板 §4.2 的三类之外）

**合格标准：能指出一条具体的、这个项目里真实存在的、违反它就会出事的路径。** 说不出来的，删掉那条槽位，不要留一条泛泛的禁令占位。

反例（不合格）：「不得引入安全漏洞」—— 指不出路径，无法断言，agent 无法据此改变任何一个决定。

---

## 3. 保护性默认怎么选

模板 §3 给了常见场景的对照表。表里没有的场景，按这条规则推：

> **选那个"猜错了也不产生对外后果、且回滚只需要一个脚本"的行为。**

两侧代价不对称，所以判断是明确的：

| | 猜少了 | 猜多了 |
|---|---|---|
| 状态迁移 | 后加一条边 | 已发生的迁移无法撤销 |
| 自动化动作 | 漏提醒 = 延迟 | 错误自动释放 = 资损 |
| 权限码 | 后拆码 = 一次迁移 | 已授出的权限收不回 |
| 派生字段 | 显示"未测量" | 假数据污染下游全部结论 |

登记时 **`rollbackCost` 字段必填**：写清"猜错了改回来要动什么"。写不出来说明基线选错了 —— 这个字段的作用不是文档，是逼你在登记那一刻就验证基线是否可回滚。

---

## 4. 断言表要如实填

模板 §6 的表由 checker 检查 5 强制：每条禁令必须有行，状态必须是 ✅ / ⚠️ 部分 / ⚠️ 技术债 三者之一。

**没有"待补"。** 待补就是 ⚠️ 技术债，如实标。

几个容易填错的：

- **`N-01` 只能是"⚠️ 部分"。** 字典内有断言（`source` 必填 + 必须解析），散文区没有。填 ✅ 是谎报；填技术债是低估。如实写"字典内有、散文只能人工审查"。
- **"数据库层只许 INSERT"这类，要写清强制层。** 只在 ORM/Mapper 层拒绝，就写"⚠️ 部分：应用层拒绝，数据库无 trigger/grant"。写 ✅ 会让下一个人以为绕过 ORM 也安全。
- **断言必须写明在哪跑。** 一条"生成物与签入副本比对"的断言，如果生成物写在别的仓库，规格库 CI 里结构上跑不了 —— 这种情况两边 CI 都是空的，而 `package.json` 里那条 script 看起来完全正常。挂闸门的位置见 `SKILL.md §2`。

允许有技术债，不允许假装没有。这张表的价值全在于它是可信的。

---

## 5. 消费仓库那两个薄文件

规格库 `CLAUDE.md` 承载全部纪律；消费仓库放 `CLAUDE.md`（纯指针）+ `AGENTS.md`（本地约定）。两份模板和每一槽的说明在 `templates/L1/consuming-repo-stubs.md`。

指针那层不能省：**agent 不会去读它不知道存在的文件。** 消费仓库里没有指针，agent 读不到真相源，只会凭空补全 —— 而它补全的东西看起来和真规格一样。
