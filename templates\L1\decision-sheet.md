<!-- 模板：decisions/D-<nn>-<短名>.md（L1）
     一条决策一份。八节固定，不适用的节写"不适用"并说明理由，不要删节 ——
     删掉的节读者无法区分"不适用"和"忘了写"。 -->

# D-<nn> · <要定什么>

> 状态：<未定案 / 有会议方向 / 已定案>
> 参会：<角色>
> 卡住的交付物：<列表；没有就写"无 —— 那么这条不该是决策">

---

## 1. 问题

<一句话说清要选什么。不写背景铺垫。>

<如果这一节写不到三行就说清，说明它其实是个缺口（规则已存在只是没写下来），
 应该登记 G-* 而不是开裁决单。>

---

## 2. 候选临时基线（**不是已定案，也不是实现授权**）

<方案 X。只有负责人明确授权、且不会产生不可撤销后果时才可实现；否则只记录候选，依赖动作保持阻塞。若获授权，说明它为何容易迁移到其他候选方案。见 §3。>

<不要在这一节论证方案 X 业务上更优 —— 那是裁决会的事。
 这一节只论证它可回滚。>

### 2.1 会议方向（<日期>）

<开过会才写这一小节，没开过就删掉整个 2.1。>

方向：<一句话>

待补：
- <项>

在以上各项冻结前，代码 <不实现某行为，只做某保护性行为>。

**有"待补:"就还没关闭。** 本文件顶部的状态保持"有会议方向"。

---

## 3. 各方案的迁移代价（从当前基线出发）

| 目标方案 | 要改什么 | 代价 |
|---|---|---|
| <A（当前基线）> | — | — |
| <B> | <3 张表 + 1 个迁移脚本> | 低 |
| <C> | <重建归属树 + 数据回填，不可逆> | 高 —— 若倾向 C，现在就要说 |

这张表有两个作用：让裁决会看得见"再拖一周的成本"，以及让倾向高代价方案的人**必须现在开口**。

---

## 4. 各方案的业务影响

| | <A> | <B> | <C> |
|---|---|---|---|
| <维度 1> | | | |
| <维度 2> | | | |

<这一节是给裁决会的材料，可以有倾向，但不许写成结论。>

---

## 5. 当前基线已经落到哪里

| 位置 | 内容 |
|---|---|
| `<contracts/...>` | <已按方案 A 出的内容> |
| `<ddl/...>` | <已建的表 / 预留的列> |

预留列、预留表要在这里点名 —— 否则下一个 agent 看到一个没人用的列，会当成废弃列删掉。

---

## 6. 与缺口的关系

<列出被这条决策牵连的 G-*，或写"无"。
 决策关闭时，这些缺口不会自动关闭 —— 各自按 10 步程序单独关。>

---

## 7. 关闭时必须回填的地方

```
- [ ] 真相源：新增/修订规则，拿到正式 ID
- [ ] contracts/*.yaml 按结论改完
- [ ] 重新生成（.md 生成区 + generated/）
- [ ] DDL：新增迁移文件，不改已发布基线
- [ ] 验收条件已补
- [ ] 追溯记录已补
- [ ] 被阻塞交付物的阻塞标记已解除
- [ ] 本文件状态改为"已定案"，写明日期与规则 ID
- [ ] 记入决策关闭日志
- [ ] 删除代码里相关的 // 基线标注
```

---

## 8. 关闭记录

<定案后填：日期 / 结论 / 规则 ID / 谁拍的。未定案就写"未关闭"。>
