# 访谈脚本

进入访谈阶段，或素材不足、要判断该登记缺口还是该问人时，读这份。

访谈的产物不是"更完整的文档"，是**一份能区分「素材里有」和「素材里没有」的文档**。第二件事比第一件重要 —— 因为下游 agent 无法区分二者，它会把两者一律当真相。

---

## 0. 唯一的原则

> **访谈是抽取，不是补全。**

每写下一条业务事实，它必须能标注为：

- `来自素材 §N`（能指到素材里的具体位置）
- 或 `已登记 G-xx`（明确承认缺失）

**没有第三个选项。** 不允许"根据常识应该是"、"行业惯例是"、"先按 3 次写着"。

编造一个不存在的规则编号比不写更糟：它看起来有依据，会被下游当真，而且不会被任何检查抓到。

---

## 1. 阶段 A：抽取

### A1 先读完全部素材再问第一个问题

素材通常是会议记录、PRD 草稿、原型、竞品截图、旧系统代码。**读完再问。** 问素材里已经写着的问题，会让用户开始跳读你后面的问题 —— 那时真正重要的那个问题也被跳过了。

### A2 抽取顺序

```
1  名词 → 术语表（一个概念一种写法，现在就定，后面不许改）
2  名词的状态 → 状态机（列出状态，先不连边）
3  动作 → 谁能做（权限码的雏形）
4  失败 → 错误码（每个失败态前端要怎么分支）
5  数字 → 全部标红等待确认（阈值/次数/金额/比例/超时/数量）
6  外部系统 → 每一个的非确定性行为（超时、未知结果、幂等语义）
```

第 5 步的产出通常有几十条，而素材里往往一条都没有明确写。这是缺口的主要来源。

### A3 抽取时的提问格式

一次问一组同类问题，每个问题带上**默认值和它的代价**：

```
关于发布失败后的重试，素材 §3.2 提到「失败可以重试」，但没写次数。

我准备按「只允许重试一次」实现（用唯一索引强制），理由是：
- 猜多了：已经发生的重复发布无法撤销
- 猜少了：加一次重试是改一个索引

如果次数不是 1，现在告诉我；如果还没定，我登记为 G-xx，代码按 1 次实现并标注。
```

这个格式让用户可以只回答"对"或一个数字，而不必先理解整个上下文。**不带默认值的开放问题回复率最低。**

### A4 阶段 A 结束前的强制自检

问自己：**这一阶段我写下的东西，哪些是素材里没有的？**

下列每一项逐个过，每项必须已标注来源或已登记：

枚举值 · 状态迁移 · 阈值 · 公式 · 价格/成本 · 重试次数 · 抽样比例 · 权限边界 · 平台/供应商名 · **数量** · 超时 · 币种/单位 · SLA 数字

**数量最容易滑过去。** "支持 8 个平台"里的 8、"七项基线检查"里的七 —— 素材里往往并没有这个数，是你在整理时凑出来的。凑出来的数量会变成 DDL 里的表、UI 里的列、测试里的断言，最后没人记得它是凭空来的。

---

## 2. 阶段 B：冻结字典（不等任何决策）

**这一步不需要任何产品决策关闭。** 枚举值、错误码、权限码几乎不依赖"选方案 A 还是 B"的结论。实测收益：约 60% 的一期工作量因此不被未定案决策阻塞。

冻结的含义：写进 `contracts/*.yaml`，每条带 `source`，从此新增值必须改 yaml（CI 强制）。

会在这一步暴露的典型问题：

| 现象 | 处理 |
|---|---|
| 同一概念素材里三种叫法 | 现在选一个，其余全部改。不要留"别名" |
| 某状态没有任何出边 | 先确认是终态还是缺口。是缺口就 `terminal: true` + 登记 |
| 两个错误码语义重叠 | 合并，或写清区分依据。一码两义会让前端误渲染 |
| 高危权限清单说不清 | 这是必须问人的，不能猜 —— 多发一个权限是已授出的权限 |

---

## 3. 阶段 C：先出 `CLAUDE.md` 和 checker

内容与理由见 [DISCIPLINES.md](DISCIPLINES.md)。这里只强调访谈层面的一件事：

**`CLAUDE.md` 的领域禁令要在访谈中问出来，不能照抄。** 问法：

```
这个项目里，哪些操作一旦做错会产生不可撤销的对外后果？
（付款、发布到外部平台、对客户发信、下单、扣费）
```

每个回答对应一条领域禁令。回答不出来的项，不要写禁令 —— 一条指不出真实风险路径的禁令，只会把 agent 的注意力引到不存在的地方。

---

## 4. 阶段 D：处理未定案决策

### D1 什么算"决策"而不是"缺口"

| | 缺口 `G-*` | 决策 `D-*` |
|---|---|---|
| 性质 | 规则存在，只是没写下来 | 规则还不存在，需要有人拍 |
| 谁能答 | 业务/产品能查到或想起来 | 需要开会、权衡、可能改变架构 |
| 典型 | 重试次数、某状态的出边、公开边界的判定 | 客户与项目的归属根、分数口径、脱敏范围 |
| 处理 | 登记 + 保护性默认，继续开发 | 裁决单 + 最省回滚基线，继续开发 |

两者都**不阻塞开发**。区别只在需要谁参与。

### D2 最省回滚基线

被决策卡住时，实现"迁移到**任何**其他候选方案都只需要一个脚本"的那个方案，并把迁移代价表写进裁决单：

```markdown
## 3. 各方案的迁移代价（从当前基线出发）

| 目标方案 | 要改什么 | 代价 |
|---|---|---|
| A（当前基线） | — | — |
| B | 3 张表 + 1 个迁移脚本 | 低 |
| C | 重建事实归属树 + 数据回填 | 高 —— 若倾向 C，现在就要说 |
```

这张表的作用不只是记录：它让裁决会看得见"再拖一周的成本是多少"，也让选 C 的人必须现在开口。

### D3 「会议方向」中间态

会开了、方向有了、但细节没定 —— 这是最容易被记成"已关闭"的状态。**不要。** 用固定写法：

```markdown
### 2.1 会议方向（2026-08-18）—— 不是已定案

方向：超过 7 天自动结束申诉并释放活跃位。

待补：
- 终态叫什么
- 原因码枚举
- 是否允许再申诉；允许几次

在以上三项冻结前，代码不实现自动释放，只产生人工提醒。
```

有"待补:"就还没关闭。`status` 保持 `open`。这个写法让项目能记录进展**而不谎报关闭**。

---

## 5. 阶段 E / F：契约与页面

这两阶段的访谈量应该很小。**如果还在大量提问，说明阶段 A 抽取不完整** —— 回去补 A，不要在契约层边写边猜。

阶段 E 唯一必须问的一类：**外部系统的非确定性行为**。素材几乎从不写这些，而它们决定了状态机有几个态：

```
调这个第三方接口，超时之后：
1. 动作可能已经生效了吗？（决定有没有「未知」态）
2. 有查询接口能问结果吗？（没有 → 「未知」是终态，只能人工）
3. 重复调用会重复生效吗？（决定幂等键怎么设计）
4. 失败了钱退不退，什么时候退？（决定要不要对账）
```

四个问题的答案直接决定状态机、幂等键、错误码和对账需求。**没有答案就不要设计这一段** —— 对第三方结果做乐观假设是资损的最常见成因。

---

## 6. 收尾：交付前必须如实汇报的

访谈全程结束时，把这几项直接写给用户，不要藏在文档里：

1. **我登记了哪些缺口**（`G-*` 列表 + 每条的保护性默认行为）
2. **哪些量是我整理时凑出来的**（尤其是数量），逐条说明依据或承认没有依据
3. **哪些禁令只能人工审查**（没有机器断言的那些，计入技术债）
4. **哪些决策在拖着，各自的迁移代价**

第 2 项是最容易被省略的，也是唯一能防住"凑出来的数字变成 DDL"的动作。
